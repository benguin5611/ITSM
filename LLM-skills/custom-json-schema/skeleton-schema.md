# Skeleton Schema

> **This file is one of two pluggable inputs for the JSON Schema skill.**
> Replace the content below with an annotated seed example of a well-formed
> schema in your format. The skill starts every new build from this skeleton.
>
> Include two things:
>
> 1. **The JSON skeleton** — a complete, minimal, valid schema in a fenced
>    code block. Not a trivial hello-world, but not a full production schema.
>    The smallest example that exercises all important structural patterns.
>
> 2. **Annotations** — explain what's fixed scaffolding (must be in every
>    schema) vs. what's example content (replace with real fields). Call out
>    any non-obvious defaults that exist to prevent specific failures.
>
> Be specific in annotations. "This property is important" is useless.
> "This property prevents X failure when Y happens" is useful.

---

*Replace everything below this line.*

---

## Skeleton

Paste your skeleton JSON here:

```json
{
  "example": "Replace this with your actual skeleton schema"
}
```

## Annotations

Explain the skeleton:

- **Fixed scaffolding** — which parts must be in every schema, regardless of content, and why
- **Example content** — which parts are placeholders to be replaced with real fields
- **Non-obvious defaults** — any values that look arbitrary but exist to prevent specific failures
